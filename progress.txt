## Codebase Patterns
- Next.js 14 with App Router, TypeScript, Tailwind CSS, src/ directory structure
- Path alias: `@/*` maps to `./src/*` in tsconfig
- ESLint config extends `next/core-web-vitals` and `next/typescript`
- Quality checks: `npm run lint`, `npx tsc --noEmit`, `npm run build`
- Dependencies: @supabase/supabase-js, @supabase/ssr, @monaco-editor/react, node-cron, recharts
- `data/sources/` is excluded from tsconfig to prevent cloned repo TS files from breaking typecheck
- Use `Array.from()` instead of spread on iterables (Map, Set) to avoid downlevelIteration errors
- Run scripts with `npx tsx scripts/foo.ts` (tsx installed as devDependency)
- Problem data: 150 problems in data/problems.json; 143 from neenza (clean), 7 premium from krmanik (basic metadata only)
- Join key across repos: LeetCode slug extracted from URL
- Types: import from `@/types` — Problem, UserProgress, AIReviewResponse, SRSResult
- Problem loader: import from `@/lib/problems` — server-side only (uses fs); cached in-process
- Supabase browser client: import `createClient` from `@/lib/supabase/client` (for client components)
- Supabase server client: import `createClient` from `@/lib/supabase/server` (async, for server components/API routes)
- Middleware refreshes auth session on every request; matcher excludes static files
- Dashboard layout: server component checks auth in layout.tsx, client Sidebar component handles navigation/logout/mobile menu
- Dashboard routes: /dashboard, /dashboard/practice, /dashboard/log, /dashboard/progress
- API route auth pattern: `const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser();` — return 401 if !user
- SRS scheduling: import `calculateNextRevisionDate(score)` from `@/lib/srs` — returns `{ next_revision_date, days_to_add }`
- Submissions API: POST /api/submissions (create), GET /api/submissions (list with ?limit, ?offset, ?problem_id, ?is_self_reported)
- Problem data must be loaded server-side and passed as props to client components (fs-based loader)
- Practice page pattern: server component loads data → client component handles interactivity (state, filtering, search)
- Code execution API: POST /api/run-code accepts { code, input } and returns { stdout, stderr, executionTime, success }
- Use `ChildProcess` type from `child_process` and optional chaining on stdio streams (`child.stdout?.on`) to satisfy TypeScript
- Monaco editor: lazy-load with `dynamic(() => import("@monaco-editor/react"), { ssr: false })` — must disable SSR
- EditorPane receives `problem` and `onAnalyze` callback; pre-populates code from `problem.code_snippets["javascript"]`
- Practice page 4-column layout: categories (w-48) | problem list (w-64) | description pane (flex-1) | editor pane (flex-1)

## 2026-03-03 14:56 - US-001
- Initialized Next.js 14 project scaffolding with all required dependencies
- Files changed:
  - package.json (added @supabase/supabase-js, @supabase/ssr, @monaco-editor/react, node-cron, recharts, @types/node-cron)
  - .env.local.example (created with all placeholder env vars)
  - .gitignore (added data/sources/ exclusion)
  - Created directory skeleton: src/app/dashboard/, src/app/api/, src/lib/supabase/, src/components/practice/, src/components/logger/, src/components/srs/, src/components/progress/, src/types/, data/, supabase/
- **Learnings for future iterations:**
  - Project uses Next.js 14.2.35 specifically (not 15.x)
  - Port 3000 may be in use on this machine; dev server falls back to 3001
  - `data/sources/` is gitignored (for merge script cloned repos), but `data/problems.json` should be committed
  - scripts/ directory already existed from project setup
---

## 2026-03-03 15:11 - US-002
- Implemented unified problem bank merge script
- Files changed:
  - scripts/merge-problems.ts (new — clones 3 repos, merges into unified schema)
  - data/problems.json (new — 150 problems with full metadata)
  - tsconfig.json (added data/sources to exclude)
  - package.json + package-lock.json (added tsx devDependency)
- **Learnings for future iterations:**
  - 7 problems are LeetCode Premium (encode-and-decode-strings, walls-and-gates, graph-valid-tree, number-of-connected-components-in-an-undirected-graph, alien-dictionary, meeting-rooms, meeting-rooms-ii) — these have minimal descriptions and no code snippets
  - neenza/leetcode-problems has the cleanest data (plain text, structured examples, code_snippets as dict)
  - krmanik/Anki-NeetCode has raw LeetCode GraphQL dumps with HTML content and null fields for premium
  - neetcode-gh/leetcode `.problemSiteData.json` has 450 problems total, use `neetcode150: true` filter for NeetCode-150
  - The `code` field in .problemSiteData.json (e.g., "0217-contains-duplicate") maps directly to neenza filenames
  - GPG signing may timeout in this environment — use `--no-gpg-sign` for commits
---

## 2026-03-03 15:13 - US-003
- Defined TypeScript interfaces and problem loader utility
- Files changed:
  - src/types/index.ts (new — Problem, ProblemExample, UserProgress, AIReviewResponse, SRSResult interfaces)
  - src/lib/problems.ts (new — getAllProblems, getProblemBySlug, getProblemsByCategory, getProblemsByDifficulty, getCategories)
- **Learnings for future iterations:**
  - Problem loader uses fs.readFileSync for server-side usage — only works in server components and API routes, not client components
  - Module-level cache (`cachedProblems`) avoids re-reading the JSON file on every call within the same process
  - `getCategories()` returns sorted array of 18 unique categories using `Array.from(Set)` pattern (per codebase patterns)
  - `code_snippets` is typed as `Record<string, string>` since available languages vary per problem (premium problems may have empty snippets)
---

## 2026-03-03 15:15 - US-004
- Created Supabase SQL schema file with full database definition
- Files changed:
  - supabase/schema.sql (new — profiles table, user_progress table, trigger, indexes, RLS policies)
- **Learnings for future iterations:**
  - Schema is a standalone SQL file for manual application in Supabase SQL Editor — no migration tooling
  - The `handle_new_user()` trigger function uses `SECURITY DEFINER` to bypass RLS when auto-creating profiles
  - profiles table has an INSERT policy for completeness but profile creation is handled by the trigger (which runs as definer)
  - Two composite indexes: `idx_user_progress_revision` (partial, for SRS queue queries) and `idx_user_progress_user_problem` (for history lookups)
  - RLS policies cover SELECT, INSERT, UPDATE for both tables — no DELETE policies needed per requirements
---

## 2026-03-03 15:18 - US-005
- Set up Supabase client libraries and auth middleware
- Files changed:
  - src/lib/supabase/client.ts (new — browser client using @supabase/ssr createBrowserClient)
  - src/lib/supabase/server.ts (new — server client using @supabase/ssr createServerClient with Next.js cookie handling)
  - src/middleware.ts (new — auth session refresh middleware with matcher config)
- **Learnings for future iterations:**
  - @supabase/ssr v0.9.0 uses `getAll`/`setAll` cookie methods (not the deprecated get/set/remove)
  - Server client `createClient()` is async because `cookies()` from next/headers is used
  - `setAll` in server components will throw (read-only cookies) — the try/catch is expected; middleware handles the actual refresh
  - Middleware matcher regex excludes static files (_next/static, _next/image, favicon.ico, image extensions)
  - GPG signing disabled for commits in this environment (use `--no-gpg-sign`)
---

## 2026-03-03 15:20 - US-006
- Built login page, auth callback route, and root page redirect
- Files changed:
  - src/app/login/page.tsx (new — email input form with magic link via signInWithOtp)
  - src/app/auth/callback/route.ts (new — exchanges auth code for session, redirects to /dashboard)
  - src/app/page.tsx (replaced — server component that redirects auth users to /dashboard, shows sign-in link for unauth)
- **Learnings for future iterations:**
  - Login page is a client component ("use client") since it needs useState and form interaction
  - Auth callback uses server-side createClient and redirects via NextResponse.redirect
  - Root page uses server component with `redirect()` from next/navigation for auth redirect
  - emailRedirectTo in signInWithOtp must point to /auth/callback for the code exchange flow
  - The root page (/) is now dynamic (ƒ) due to server-side auth check
---

## 2026-03-03 15:23 - US-007
- Created authenticated dashboard layout with sidebar navigation
- Files changed:
  - src/app/dashboard/layout.tsx (new — server component, auth check with redirect, renders Sidebar + children)
  - src/components/dashboard/Sidebar.tsx (new — client component with nav links, active state, logout, responsive mobile menu)
  - src/app/dashboard/page.tsx (new — placeholder "Dashboard Home" page)
  - src/app/dashboard/.gitkeep (removed — no longer needed)
- **Learnings for future iterations:**
  - Dashboard layout uses server component for auth check and passes user email to client Sidebar
  - Sidebar is a client component ("use client") for interactivity (logout, mobile menu toggle, active link highlighting via usePathname)
  - Active link detection: exact match for /dashboard, startsWith for sub-routes (/dashboard/practice, etc.)
  - Mobile responsiveness: sidebar slides in/out with translate-x transition, overlay backdrop, hamburger button fixed at top-left
  - Extra top padding (pt-16) on mobile main content to avoid overlap with fixed hamburger button
  - The /dashboard route is now dynamic (ƒ) due to server-side auth check in layout
---

## 2026-03-03 15:26 - US-008
- Implemented SRS calculation library and submissions CRUD API
- Files changed:
  - src/lib/srs.ts (new — calculateNextRevisionDate function: score 1-2 → +1 day, score 3 → +4 days, score 4-5 → +14 days)
  - src/app/api/submissions/route.ts (new — POST creates submission with SRS scheduling, GET lists with pagination/filters)
- **Learnings for future iterations:**
  - API routes use `await createClient()` from `@/lib/supabase/server` and `supabase.auth.getUser()` for auth
  - Supabase `.insert().select().single()` returns the created record directly
  - Use `query.range(offset, offset + limit - 1)` for pagination (inclusive range)
  - Query filters can be chained conditionally: `if (filter) query = query.eq(...)`
  - The submissions API accepts optional fields by checking `body[field] !== undefined` to allow falsy values
---

## 2026-03-03 15:28 - US-009
- Built practice page with problem browser and category sidebar
- Files changed:
  - src/app/dashboard/practice/page.tsx (new — server component, loads problems/categories from lib, passes to client component)
  - src/components/practice/ProblemBrowser.tsx (new — client component with category sidebar, search input, problem list with difficulty badges and Blind75 indicator)
- **Learnings for future iterations:**
  - Problem data is loaded server-side via `getAllProblems()`/`getCategories()` and passed as props to client components (since `@/lib/problems` uses `fs`)
  - ProblemBrowser maintains `selectedProblem` state — US-010 will need to lift this or access it to render the ProblemPane alongside
  - Category counts are computed via `useMemo` from the problems array; categoryCounts map used for sidebar badges
  - The practice page renders at `/dashboard/practice` as a dynamic route (ƒ) due to the dashboard layout auth check
  - Difficulty colors: Easy=green, Medium=yellow, Hard=red using Tailwind utility classes with dark mode variants
---

## 2026-03-03 15:31 - US-010
- Built ProblemPane component and integrated it into the practice page as a right panel
- Files changed:
  - src/components/practice/ProblemPane.tsx (new — displays problem title, badges, description, examples, constraints, collapsible hints, external links)
  - src/components/practice/ProblemBrowser.tsx (modified — added ProblemPane import, changed to 3-column layout: categories sidebar w-52, problem list w-80, description pane flex-1)
- **Learnings for future iterations:**
  - Hints from problems.json contain HTML (e.g., `<code>` tags) — rendered with `dangerouslySetInnerHTML` in the hints section only
  - ProblemPane resets `revealedHints` state when a new problem is selected (no effect cleanup needed since it's managed per-component re-render)
  - The 3-column layout uses flex with fixed widths for sidebar (w-52) and problem list (w-80), with the description pane taking remaining space (flex-1)
  - NeetCode video link constructed from `neetcode_video_id` field: `https://www.youtube.com/watch?v={id}`
  - Problem title text uses `truncate` class in the list to prevent overflow in the narrower panel
---

## 2026-03-03 15:33 - US-011
- Implemented code execution API route at /api/run-code
- Files changed:
  - src/app/api/run-code/route.ts (new — POST endpoint that executes JavaScript code in isolated temp dir via Node.js child process)
- **Learnings for future iterations:**
  - `spawn` from `child_process` needs explicit `ChildProcess` type annotation when using TypeScript strict mode — the overload inference fails with certain option combinations
  - Use optional chaining on `child.stdout?.on` / `child.stderr?.on` / `child.stdin?.write` since they can be null when stdio isn't piped
  - Don't restrict `env` in spawn options (e.g., `env: { PATH: ... }`) — TypeScript ProcessEnv requires NODE_ENV and other fields; just omit `env` to inherit parent
  - `spawn` `timeout` option sends SIGTERM but doesn't guarantee process death — add a manual setTimeout with SIGKILL as fallback
  - Temp file cleanup goes in `finally` block of the POST handler, not in the executeCode function
---

## 2026-03-03 15:38 - US-012
- Built Monaco editor pane with custom input/output execution and integrated into practice page
- Files changed:
  - src/components/practice/EditorPane.tsx (new — Monaco editor with lazy loading, stdin input, run code button, output panel with success/error states, Analyze & Score button)
  - src/components/practice/ProblemBrowser.tsx (modified — added EditorPane as 4th column, tightened widths for categories w-48 and problem list w-64 to make room)
  - prd.json (US-012 passes: true)
- **Learnings for future iterations:**
  - Monaco editor MUST be dynamically imported with `ssr: false` — it uses browser APIs that crash server-side rendering
  - The `loading` prop of `dynamic()` provides a placeholder while the Monaco chunk downloads (~1MB)
  - Use `automaticLayout: true` in Monaco options so it auto-resizes when the container changes dimensions
  - The practice page now has 4 visible columns; category sidebar and problem list were compressed from w-52/w-80 to w-48/w-64 with smaller text (text-xs) to fit the editor pane
  - `onAnalyze` callback is a placeholder for US-014 AI review integration — currently logs to console
  - Output panel border uses green (success) / red (error) to provide instant visual feedback
---
